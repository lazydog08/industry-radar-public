# TASK-06 移动端 UI 和阅读体验优化

- 更新时间：2026-05-26 15:45 CST
- 负责范围：`src/web/index.html`、`src/web/app.js`、`src/web/styles.css`；未改导出器、NAS 脚本、Bark 或 API。
- TASK-03 头版样式补充：选择 E 风格「报纸头版 / 情报日报」，本次只调整 `src/web/styles.css` 和本文档。

## 优化目标

让首页在 iPhone 竖屏下更像“每天刷一眼的行业情报”，优先保证快速判断、纵向阅读、可点击溯源和评分含义清楚。

E 风格要求首屏像一份行业情报日报：先看到有双线规则的 masthead、头版头条、分区简报和编辑精选，再进入下方工作台。

## 已完成调整

- 首屏信息密度：手机端概览指标改为 2 列，减少一屏内纵向占用。
- 一图读懂：手机端降低内边距和固定高度，三段内容改为单列卡片，评分条更窄，避免标题和正文挤压。
- 评分体系：保留 `A 重点 / B 观察 / C 背景 / D 暂存` 彩色胶囊，手机端图例压缩为 2 列，隐藏次级提示，先让等级含义可扫读。
- 来源链接：一图读懂底部继续使用短来源标签，移动端限制域名宽度；知识卡保留完整可点击来源，长 URL 在手机端折叠显示，避免撑破版面。
- 筛选区：默认只保留“关键词 + 搜索”，分类、来源、实体、标签和反馈筛选收进“高级筛选”，减少手机首屏干扰。
- 情报分区：每个分区默认显示 4 条，超过 4 条时提供“显示剩余 / 收起”，避免第 5 条以后在首页永久不可见。
- 报告归档：默认显示 8 份，超过 8 份时提供“显示全部 / 收起”，保留首屏简洁同时不丢历史报告。
- 只读反馈：线上只读模式的反馈说明移到按钮旁边，用户能直接知道收藏、跟踪、已用、不感兴趣不会写入线上页面。
- 知识卡：正文行高、段落间距、评分卡和操作按钮在手机端重新压缩，适配 200-300 字知识卡阅读。
- 报纸头版：使用纸张底色、墨色正文、红色头版强调和蓝色链接强调；`RADAR DAILY / 情报日报` 使用报纸 masthead 视觉。
- 头版布局：桌面端 `frontpage` 采用头条 + 简报栏两列，编辑精选以三条报纸栏目承接；搜索和高级筛选进入头版简报栏，分区列表、知识卡和归档作为下方工作台，视觉层级弱于头版。
- 头版交互：头条正文可点击选中事件，同时提供明确的“打开知识卡”按钮；来源链接保持普通链接语义，不再放在 button-like 容器内。
- 移动端预期：760px 及以下改为单列阅读流，概览和头版分区指标保持 2 列，头条字号下调，筛选控件和按钮保持 44px 点击高度，不应出现横向滚动。

## 验证方法

- TASK-03 静态检查：`git diff --check`
- 静态检查：`node --check src/web/app.js`
- 类型检查：`pnpm typecheck`
- 浏览器验证：使用本地页面 `http://localhost:3887/`，分别检查 390px 手机宽度和 1200px 桌面宽度。
- E 风格移动验证：补充检查 760px、390px 两档宽度，确认 masthead、头版头条、头版指标、编辑精选、筛选区和工作台均无横向溢出。
- E 风格集成验证：2026-05-26 使用隔离 demo 服务 `127.0.0.1:3895/3896` 复查，静态模式和 API 模式均无 console error；搜索视图不保留可点击头版；API 模式搜索后点击反馈不会把首页头版刷回；390px 宽度无横向溢出。

## 集成注意

- 本任务只改前端交互、样式和文档，未触碰数据抓取、静态导出、NAS 脚本或 Bark。
- 若 TASK-02 后续改动页面结构，请保留以下语义 class，移动端样式会继续生效：`infographic`、`score-pill`、`source-chip`、`filters`、`event-card`、`knowledge-fields`。
- 若 TASK-02 继续调整头版结构，请保留以下语义 class：`frontpage`、`frontpage-lead`、`frontpage-brief`、`frontpage-stat-grid`、`frontpage-lead-head`、`frontpage-angles`、`editorial-strips`、`editorial-strip-item`。
- 静态模式的 `events.json` 懒加载逻辑不应移除；分区展开只作用于当前首页事件，不替代搜索/筛选的全量事件加载。

## 遗留风险

- 高级筛选使用原生 `details/summary`，视觉足够轻，但还没有加“已启用筛选数量”的摘要提示。
- 还没有引入真实截图回归测试；建议最终集成阶段补 390px 和 1200px 的截图基线。
- 本次浏览器截图抓取在 Codex in-app browser 中超时；已用 DOM、控制台、交互和 390px/桌面宽度检查替代。后续若要做视觉回归，可补 Playwright/截图依赖。
